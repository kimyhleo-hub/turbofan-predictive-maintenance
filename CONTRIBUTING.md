# 협업 방식

> 프로젝트는 완료됐다. 아래는 진행 당시 실제로 따랐던 협업 방식을 그대로 남긴 기록이다.

이 프로젝트는 단계별로 함께 진행했다. 한 라운드에 한 단계를 각자 수행한 뒤,
공유·비교하고 **하나의 통합본으로 합쳤다**. 모두가 전 과정을 직접 다뤄 보는 것이 목표였다.

## 폴더 구조

- `notebooks/` — **통합본 (레포의 정본)**. 01~05 전부 실행된 상태로 유지한다.
- `reports/` — 각 단계의 보고서(노트북과 1:1 대응) + 발표 슬라이드.
- `scripts/` — 오래 걸리는 실험(LSTM 학습, 튜닝, SA, 롤링)의 체크포인트형 스크립트.
- `src/pipeline.py` — 공용 함수(RUL 예측·스케줄링).

## 라운드 흐름

1. **개별 작업** — 각자 자기 환경에서 이번 라운드 단계를 수행한다.
2. **공유** — 결과물(노트북·그림·수치)을 팀 채널로 보낸다.
3. **비교** — 접근 방식, 수치, 발견한 점을 서로 대조한다.
4. **통합** — 담당자가 합의된 내용을 `notebooks/` 통합본과 해당 보고서에 반영해 커밋한다.
5. 다음 단계로 넘어간다.

> 개인 작업 폴더(`members/`)는 운영하지 않는다. 각자 결과를 보내면 **통합본에 직접 반영**한다.

## 단계 구성

| 단계 | 노트북 | 보고서 |
|---|---|---|
| 1 | `01_eda` | `eda_report.pdf` |
| 2 | `02_preprocessing` | `preprocessing_report.pdf` |
| 3 | `03_rul_model` | `rul_model_report.pdf` |
| 4 | `04_optimization` | `scheduling_report.pdf` |
| 5 | `05_evaluation` | `final_report.pdf` (전체 종합) |

각 단계의 구체 작업은 [RESEARCH_PLAN.md](RESEARCH_PLAN.md)를 따른다.

## 규칙

- **통합본은 항상 실행 가능한 상태로 유지**한다(출력이 담긴 채 커밋).
- 단계 간 인터페이스(컬럼·파일명)는 RESEARCH_PLAN의 공유 인터페이스 표를 따른다.
- 데이터 원본은 커밋하지 않는다([data/README.md](data/README.md)).
- 검증(누수 차단·시드 재현·공정 비교)을 통과한 결과만 반영한다.
- 커밋 메시지는 무엇을 했는지 한 줄로 적는다. 예: `feat: 전처리 v2 특징 추가 (test 11.65)`.

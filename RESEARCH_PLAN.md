# 연구 계획서

프로젝트 개요는 [README](README.md)를 참고한다. 이 문서는 각 단계를 실제로 구현하기 위한 실행 계획이다.

---

## 파이프라인

```
데이터 → RUL 예측 → 예측 RUL을 정비 마감으로 변환 → 스케줄 최적화 → 실제 고장으로 재현 평가
```

각 단계는 노트북에 대응한다: `01_eda` → `02_preprocessing` → `03_rul_model` → `04_optimization` → `05_evaluation`. 앞 단계의 산출물이 뒤 단계의 입력이 되므로, 인터페이스(컬럼명·파일명)를 먼저 맞춘 뒤 병렬로 진행한다. 협업 방식(라운드제)은 `CONTRIBUTING.md` 참고.

**공유 인터페이스**

| 산출물 | 생산 | 소비 | 형식 |
|--------|------|------|------|
| 전처리 데이터 | 01/02 | 02 | `data/processed/` CSV |
| `predicted_RUL` | 02 | 03 | 엔진별 RUL 예측(사이클) |
| 정비 스케줄 | 03 | 04 | 엔진별 정비 창 배정 |
| 실제 고장 시점 | 02 | 04 | 정답 RUL에서 산출 |

---

## 1단계 — 데이터와 전처리 (`01_eda`, `02_preprocessing`)

**목표** — 원본 C-MAPSS를 모델 입력과 RUL 라벨로 변환하고, 열화 패턴을 확인한다.

**작업**

1. 로드 — `train_FD001.txt`를 26개 컬럼으로 읽는다.
2. EDA — 엔진 수명 분포, 센서별 변동(상수 센서 확인), 사이클에 따른 센서 드리프트를 시각화한다.
3. 유효 센서 선택 — FD001에서 상수인 s1·s5·s10·s16·s18·s19를 제거해 15개를 남긴다.
4. 스케일링 — 센서 스케일 차이를 정규화한다(z-score 또는 min-max).
5. 윈도우 특징 — 각 시점에서 최근 N사이클의 평균·표준편차·기울기를 특징으로 만든다(초기값 N=20에서 시작해 조정).
6. RUL 라벨 — `RUL = 총수명 − 현재 cycle`, 상한 clip(초기값 125).
7. 분리 — **엔진 단위**로 train/valid를 나눈다(`GroupShuffleSplit` 등). 같은 엔진의 서로 다른 사이클이 train과 valid에 동시에 들어가지 않게 한다.

**산출물** — EDA 그림, `data/processed/`의 학습용 데이터, RUL 라벨.

**주의** — 무작위 행 분할은 미래 정보 유입으로 성능을 과대평가한다. 반드시 엔진 단위로 분리한다.

---

## 2단계 — RUL 예측 (`03_rul_model` — 앙상블 + 선택 LSTM)

**목표** — 센서 특징에서 RUL을 회귀하고, 전 엔진에 대한 예측을 최적화 입력으로 저장한다.

**작업**

1. baseline — 선형회귀, RandomForest.
2. 메인 — 트리 앙상블(LightGBM 또는 XGBoost). 여유가 있으면 LSTM/1D-CNN을 추가한다.
3. 평가 — RMSE, MAE, PHM08 score를 계산한다. PHM08 score는 늦은 예측(과대추정)에 더 큰 벌점을 준다.
4. 예측 저장 — 전 엔진의 `predicted_RUL`을 `data/processed/`에 저장한다.

**평가 표(채울 것)**

| 모델 | RMSE | MAE | PHM08 |
|------|-----:|----:|------:|
| 선형회귀 | | | |
| RandomForest | | | |
| 트리 앙상블 | | | |

**목표선** — 단순 모델(RF·LightGBM, 마지막 시점 예측)은 FD001 테스트 RMSE 약 16–20이 정상이다. 약 12–14는 딥러닝(LSTM)·튜닝이 필요하다. 반대로 RMSE가 지나치게 낮으면(예: 한 자리) 엔진 단위 분리에 누수가 없는지 먼저 확인한다.

---

## 3단계 — 정비 스케줄링 최적화 (`04_optimization`)

**목표** — 예측 RUL을 정비 마감으로 두고, 자원 제약 하에서 스케줄을 최적화한다.

### 3.1 세팅과 변환

계획 기간을 `T`개 정비 창으로 나눈다. 예측 RUL을 창 단위로 환산해 마감 `d_i`를 만든다(이 창까지 정비하지 못하면 고장).

### 3.2 정식화

```
결정변수
  x[i,t] = 1  엔진 i를 창 t에 정비, 아니면 0
  prevented_i = 1  if 정비 시점 m_i ≤ 마감 d_i,  else 0
  wasted_life_i = max(0, d_i − m_i)   조기 정비로 버린 잔여수명

목적함수 (두 층)
  (주)  minimize  Σ (1 − prevented_i)   또는   Σ wasted_life_i
  (부)  minimize  C_pm·(정비수) + C_fail·(고장수) + w·(조기정비 낭비)
        단  C_fail ≫ C_pm

제약
  Σ_t x[i,t] ≤ 1              엔진당 정비 1회
  Σ_i x[i,t] ≤ K       ∀t     창당 정비팀 용량
  Σ x[i,t]·part_i ≤ Stock     예비부품 재고
  m_i ≤ d_i − s_i             안전마진(RUL 불확실성)
  x[i,t] ∈ {0,1}
```

주 지표는 실제 고장으로 직접 세므로 비용 가정에 의존하지 않는다. 비용비는 부 지표에서만 쓰고 민감도로 확인한다.

### 3.3 알고리즘

- baseline — Random, EDF(마감 우선), 비용/위험 비율 greedy.
- 메인 — Simulated Annealing.
  - 해 표현: 길이 N 벡터, 각 엔진의 정비 창(0 = 정비 안 함).
  - 이웃 연산: 한 엔진의 창을 변경, 또는 두 엔진의 창을 교환.
  - 수용: 개선이면 채택, 악화면 `exp(−Δ/T)` 확률로 채택.
  - 냉각: 온도를 지수적으로 감소.
  - 제약 위반은 목적함수에 페널티로 반영하거나 이웃 생성 단계에서 배제.

### 3.4 시작 파라미터

| 파라미터 | 초기값 | 튜닝 범위 |
|----------|--------|-----------|
| 정비 창 수 `T` | 10 | 데이터 규모에 맞춰 |
| 창당 용량 `K` | 5 | 3–10 |
| 비용비 `C_fail:C_pm` | 5:1 | 3:1 – 10:1 |
| SA 초기 온도 | 5 | 1, 5, 10 |
| SA 냉각률 | 0.95 | 0.90, 0.95, 0.99 |
| SA 반복 수 | 1000 | 500, 1000, 2000 |

**산출물** — 스케줄, SA 수렴 곡선, 파라미터 튜닝 표.

---

## 4단계 — 평가 (`05_evaluation`)

**목표** — 스케줄을 실제 고장에 재현해 성과를 측정하고 강건성을 확인한다.

**작업**

1. 재현(replay) — 예측 기반 스케줄을 실제 고장 시점에 적용해 미정비 고장 수, 다운타임, 총비용을 센다.
2. 전략 비교 — Random / EDF / greedy / SA / Oracle(실제 RUL 가정, 상한). Oracle과의 격차가 예측 오차로 인한 손실이다.
3. 민감도(What-if) — `K`, 비용비, 부품 재고, RUL 불확실성을 변화시키며 결론이 유지되는지 확인한다.

**전략 비교 표(채울 것)**

| 전략 | 미정비 고장 수 | 다운타임 | 총비용 | 실행 시간 |
|------|---------------:|---------:|-------:|----------:|
| Random | | | | |
| EDF | | | | |
| Greedy | | | | |
| SA | | | | |
| Oracle | | | | |

---

## 진행 원칙

- 앞 단계의 인터페이스(컬럼·파일명)를 먼저 확정한 뒤 병렬로 진행한다.
- 최적화가 예상과 다르게 동작하면 알고리즘보다 파라미터·마감 변환·제약 정의를 먼저 점검한다.
- 비용·용량 등 데이터에 없는 값은 문헌 범위를 참고해 설정하고, 결과는 항상 민감도와 함께 보고한다.
